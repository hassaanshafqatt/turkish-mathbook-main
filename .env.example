# Supabase Configuration
VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Books Webhook Configuration (Server-side only, not exposed to client)
# This webhook URL is used to fetch the list of uploaded books
# It should be configured via environment variables and cannot be changed from the UI
BOOKS_WEBHOOK_URL=https://your-n8n-instance.com/webhook/books

# Server Port (optional, defaults to 7893)
PORT=7893
